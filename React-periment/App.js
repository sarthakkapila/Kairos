import React from "react";

const App = () => {
  return (
    <div>
      <h1>React Experiment</h1>
      <p>This is a React app for experimenting with components, state, and props.</p>
    </div>
  );
};

export default App;